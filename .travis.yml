cache:
 directories:
  - node_modules

deploy:
 github_token: $DEPLOY_GITHUB_TOKEN
 local_dir: build
 provider: pages
 skip_cleanup: true

language: node_js

node_js:
 - lts/*

sudo: false

install:
 - npm install

script:
 - webpack -p
